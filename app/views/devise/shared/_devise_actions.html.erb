<%if user_signed_in?%>
  <span>
    <p>Loggedin as: <%= current_user.email%></p>
    <input id='notification_user_id' type='hidden' value="<%= current_user.id%>">
    <%= link_to "Log out", destroy_user_session_path, :method => :delete %>
  </span>
<%end%>
